<UserControl x:Class="CalculatorWPF.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CalculatorWPF"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=local:SettingsViewModel, IsDesignTimeCreatable=False}">
    <Grid>
            <Button VerticalAlignment="Center" HorizontalAlignment="Center" Content="Info" />
            <StackPanel Margin="10">
                <CheckBox Content="AS = . DS = ,"  IsChecked="{Binding UseDotForDecimal}"/>
                <CheckBox Content="DS = . AS = ,"  VerticalAlignment="Center" IsChecked="{Binding UseDotForDecimal}"/>
            </StackPanel>
    </Grid>
</UserControl>
