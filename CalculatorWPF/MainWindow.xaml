<Window x:Class="CalculatorWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalculatorWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Display"/>
        <TextBox DockPanel.Dock="Top" Text="Entry" />

        <DockPanel DockPanel.Dock="Bottom" LastChildFill="False">
            <Button DockPanel.Dock="Left" Content="Variables" />
            <Button DockPanel.Dock="Right" Content="Settings" />
        </DockPanel>
    
        <UniformGrid Columns="5" Background="#FFEFF1F3">
            <UniformGrid.Resources>
                <Style TargetType="Button">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="FontSize" Value="24"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Margin" Value="1" />
                    <Setter Property="Background" Value="Gray"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </UniformGrid.Resources>
            
            <Button Content="7" />
            <Button Content="8" />
            <Button Content="9" />
            <Button Content="+" />
            <Button Content="back"/>
            
            <Button Content="4" />
            <Button Content="5" />
            <Button Content="6" />
            <Button Content="-" />
            <Button Content="C"/>
            
            <Button Content="1" />
            <Button Content="2" />
            <Button Content="3" />
            <Button Content="*" />
            <Button Content="("/>
            
            <Button Content="," />
            <Button Content="0" />
            <Button Content="=" />
            <Button Content="/" />
            <Button Content=")"/>
        </UniformGrid>
    </DockPanel>
</Window>
